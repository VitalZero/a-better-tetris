#include "BoardDeletingState.h"
#include "board.h"

void BoardDeletingState::Enter(Board* owner)
{}
void BoardDeletingState::Input(Board* owner)
{}
void BoardDeletingState::Update(Board* owner, float)
{}
void BoardDeletingState::Draw(Board* owner)
{}
void BoardDeletingState::Exit(Board* owner)
{}